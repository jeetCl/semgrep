name: Semgrep SAST Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 3 * * 1'  # Every Monday at 3AM UTC
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual scan'
        required: false
        default: 'Manual trigger by user'

jobs:
  semgrep:
    name: Run Semgrep Scan
    runs-on: ubuntu-latest

    permissions:
      contents: read
      security-events: write  # Required for uploading SARIF to GitHub

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Semgrep (Pro/CI)
        run: |
          curl -s https://semgrep.dev/install.sh | bash

      - name: Run Semgrep Scan and Upload to Semgrep Cloud
        run: |
          echo "Reason: ${{ github.event.inputs.reason }}"
          # Scan and upload to Semgrep Cloud
          semgrep scan \
            --config=auto \
            --json \
            --output=semgrep.json \
            --upload

          # Run again to generate SARIF for GitHub Code Scanning
          semgrep scan \
            --config=auto \
            --sarif \
            --output=semgrep.sarif
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        continue-on-error: true

      - name: Upload SARIF to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: semgrep.sarif
